{{- if .Values.adapter.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: metrics-adapter
  namespace: {{ .Release.Namespace }}
  labels:
    app: metrics-adapter-aws-cloudwatch
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.adapter.service.port }}
      targetPort: {{ .Values.adapter.service.port }}
      protocol: TCP
      name: http
  selector:
    app: metrics-adapter-aws-cloudwatch
{{- end }}
