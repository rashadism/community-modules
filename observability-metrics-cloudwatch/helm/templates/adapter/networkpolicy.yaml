{{- if and .Values.adapter.enabled .Values.adapter.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: metrics-adapter-cloudwatch
  namespace: {{ .Release.Namespace }}
spec:
  podSelector:
    matchLabels:
      app: metrics-adapter-cloudwatch
  policyTypes:
    - Ingress
  ingress:
    - ports:
        - protocol: TCP
          port: {{ .Values.adapter.service.port }}
      from:
        - namespaceSelector:
            matchLabels:
              {{- toYaml .Values.adapter.networkPolicy.observerNamespaceLabels | nindent 14 }}
          podSelector:
            matchLabels:
              {{- toYaml .Values.adapter.networkPolicy.observerPodLabels | nindent 14 }}
        {{- if and .Values.adapter.alerting.webhookRoute.enabled .Values.adapter.networkPolicy.gatewayNamespaceLabels }}
        - namespaceSelector:
            matchLabels:
              {{- toYaml .Values.adapter.networkPolicy.gatewayNamespaceLabels | nindent 14 }}
          {{- if .Values.adapter.networkPolicy.gatewayPodLabels }}
          podSelector:
            matchLabels:
              {{- toYaml .Values.adapter.networkPolicy.gatewayPodLabels | nindent 14 }}
          {{- end }}
        {{- end }}
        {{- if .Values.adapter.networkPolicy.allowProbeIPBlock }}
        - ipBlock:
            cidr: {{ .Values.adapter.networkPolicy.allowProbeIPBlock | quote }}
        {{- end }}
{{- end }}
